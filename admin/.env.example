# Copy to admin/.env — same values as repo root .env (VITE_ prefix required by Vite).
# Never commit admin/.env.

VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key_here
